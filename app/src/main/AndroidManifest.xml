<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jd.myjd.imitate2jd">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.ReplaceActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.RegActivity" />
        <activity android:name=".ui.activity.OthetActivity" />
        <activity android:name=".ui.activity.classificationActivity" />
        <activity android:name=".ui.activity.GoodsActivity" />
        <activity android:name=".ui.activity.ClassXqPageActivity" />
        <activity android:name=".ui.activity.HomeSearchActivity" />
        <activity android:name=".ui.activity.CartsActivity" />
        <activity android:name=".ui.activity.GoodXqActivity"></activity>
    </application>

</manifest>